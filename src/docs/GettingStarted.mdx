import { Meta } from "@storybook/addon-docs/blocks"

<Meta title="Getting Started" />

# @am-fernandes/ui

Design system oficial da AM Fernandes — 41 componentes React acessíveis, theming em OKLCH, fonte Google Sans Flex, light-only.

## Pré-requisitos

- **Node ≥ 20** ou **Bun ≥ 1.1**
- **React 19**
- **Tailwind CSS v4**

## Instalação

1. **Configure o registro privado** (uma vez por máquina):

```bash
# ~/.npmrc
@am-fernandes:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
```

2. **Adicione o package:**

```bash
bun add @am-fernandes/ui
# ou
npm install @am-fernandes/ui
```

## Setup

No CSS de entrada da app:

```css
/* src/index.css ou app/globals.css */
@import "@am-fernandes/ui/fonts";
@import "@am-fernandes/ui/styles";
```

Pronto. Tokens, paleta e fonte ficam ativos globalmente.

## Primeiro componente

```tsx
import { Button, Typography } from "@am-fernandes/ui"

export function Demo() {
  return (
    <div className="space-y-4">
      <Typography variant="title">Olá, AM</Typography>
      <Button onClick={() => alert("Funcionou")}>Salvar</Button>
    </div>
  )
}
```

## Conceitos

- **Light only.** Sem dark mode por escolha de produto.
- **Tokens são a API.** Consuma cor/raio/espaço via classe Tailwind (`bg-primary`, `rounded-md`), nunca hardcoded.
- **Acessibilidade ≥ AA.** Componentes seguem padrões ARIA; status badges, contraste e foco validados via `addon-a11y` no Storybook e `injectAxe`/`checkA11y` em CI.
- **API plana por padrão.** Cada componente expõe um único export com `title`/`description`/`children` (overlays) ou `items` (navegação). Slots `headerAction`/`footer`/`trigger`/`action` cobrem os edge cases sem expor subcomponentes Radix.
- **Forms sem wrapper.** Combine os controles (`Combobox`, `DateInput`, `Calendar`, etc.) com `react-hook-form` direto — todos eles aceitam `label`/`description`/`error` nativamente.

## Testes

Cobertura via [vitest](https://vitest.dev) + testing-library. Veja [Quality](?path=/docs/quality--docs) para detalhes.

```bash
bun run test
```

## Categorias

| Categoria | Count | Conteúdo |
|---|---|---|
| **Primitives** | 11 | Avatar, Badge, Button, Checkbox, Input, RadioGroup, Separator, Skeleton, Switch, Textarea, Typography |
| **Overlays** | 9 | Alert, AlertDialog, Collapsible, Dialog, Popover, Progress, Sheet, Toaster (Sonner), Tooltip |
| **Navigation** | 5 | Accordion, Breadcrumb, CommandPalette, Sidebar, Tabs |
| **Forms** | 5 | Calendar, Combobox, DateInput, DateRangePicker, TimePicker |
| **Domain** | 5 | CurrencyInput, FileUpload, InputOTP, MultiInput, PercentageInput |
| **Data** | 6 | Card, DataTable, Image, ScrollArea, Tree, Video |
| **Hooks** | — | `useIsMobile`, `useComboboxOptions`, helpers de className, BRL e bytes |
| **Foundations** | — | Cores, tipografia, espaçamento, iconografia |

Navegue pela sidebar à esquerda para ver cada componente, suas props e variantes. A aba **Hooks** documenta os helpers/hooks não-componentes expostos pelo barrel.

## Migração de apps existentes

Para apps internas que já usam cópias shadcn locais:

1. Adicione a dep + os imports CSS (passos acima).
2. Substitua um import por vez:
   ```diff
   - import { Button } from "@/components/ui/button"
   + import { Button } from "@am-fernandes/ui"
   ```
3. Para acelerar, use sed em uma página piloto antes de aplicar globalmente:
   ```bash
   find src -type f -name '*.tsx' -exec \
     sed -i 's|from "@/components/ui/\([a-z-]*\)"|from "@am-fernandes/ui"|g' {} +
   ```
4. Smoke-test a página, então delete `src/components/ui/*.tsx`.

**Gotchas conhecidas:**
- `Button` não tem mais flow de confirmação embutido — use `AlertDialog` quando precisar confirmar.
- Sem dark mode. Remova qualquer toggle de classe `.dark` que ainda exista.
- `Chart*`, `Form`/`Field`/`FieldGroup`/`FormField`, `DropdownMenu` foram removidos em v10. Use `recharts` direto para gráficos, `react-hook-form` direto para forms, e `Popover` + `Button` para menus de ação.
- Helpers `toCents`, `fromCents`, `centsToDisplay`, `percentOfTotal`, `percentFromValue`, `formatBRL` são exportados — delete `lib/currency.ts` locais. Veja a aba **Hooks** para a lista completa.

## FAQ

**Posso usar com Vite ou Next.js?** Sim. O package é ESM puro; qualquer bundler com React 19 + Tailwind v4 funciona.

**Posso adicionar variantes a um componente existente?** Use `className` (passa pelo `cn()` com tailwind-merge). Para variantes de marca, abra uma issue.

**Como contribuir?** Veja `CONTRIBUTING.md` (em breve). Para mudanças urgentes, contate o time interno.
